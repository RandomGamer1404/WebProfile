A basic WebProfile developed using Vite, tailwindCSS and three.js.

Clone the project and install dependencies:

```bash
git clone https://github.com/RandomGamer1404/EcoWave.git
cd WebProfile
npm install
npm run dev
